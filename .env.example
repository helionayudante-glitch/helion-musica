# Copia este fichero a ".env" y rellena. El .env NUNCA se sube a GitHub.
DISCORD_TOKEN=pega_aqui_tu_token_NUEVO_regenerado
# OPCIONAL — voz de DJ Helion (Piper local). Deja {out} tal cual:
# PIPER_CMD=piper --model /ruta/es_ES-davefx-medium.onnx --output_file {out}
